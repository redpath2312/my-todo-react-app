rules_version = '2';

service cloud.firestore {
    match /databases/{database}/documents {

// Only allow users access to their own own user data
    function authed() { return request.auth != null; }
    match /users/{uid} {
      allow read, write: if authed() && request.auth.uid == uid;
    }    
  }
}